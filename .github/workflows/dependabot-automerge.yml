# This workflow is managed by gh actions-lock.

name: Dependabot Auto-merge

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
  workflow_dispatch:
    inputs:
      pull_request_number:
        description: Open Dependabot pull request number
        required: true
        type: number

# Automatic runs use the event head; dispatch runs resolve the live PR. Only the short-lived App token mutates state.
permissions: {}

concurrency:
  group: dependabot-automerge-${{ github.event.pull_request.number || inputs.pull_request_number || github.run_id }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  enable-dependabot: # zizmor: ignore[secrets-outside-env] Dedicated Dependabot secrets are the documented pull_request pattern; no repository content is checked out.
    name: Enable native auto-merge (Dependabot event)
    if: >-
      github.event.pull_request.user.id == 49699333 &&
      github.repository == github.event.pull_request.head.repo.full_name &&
      startsWith(github.event.pull_request.head.ref, 'dependabot/') &&
      github.event.pull_request.draft == false &&
      github.event_name == 'pull_request' &&
      github.event.sender.id == 49699333 &&
      (
        github.event.action == 'opened' ||
        github.event.action == 'synchronize'
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Create the repository-scoped GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.DEPENDABOT_AUTOMERGE_APP_CLIENT_ID }}
          private-key: ${{ secrets.DEPENDABOT_AUTOMERGE_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write
          permission-merge-queues: write

      - name: Verify Dependabot provenance of the exact head
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          commit_json="$(gh api "repos/${REPOSITORY}/commits/${HEAD_SHA}")"
          jq -e --arg sha "$HEAD_SHA" '
            .sha == $sha and
            .author.id == 49699333 and
            .author.login == "dependabot[bot]" and
            .committer.login == "web-flow" and
            .commit.verification.verified == true and
            .commit.verification.reason == "valid"
          ' <<<"$commit_json" >/dev/null

      - name: Enable GitHub native auto-merge
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: gh pr merge --auto --match-head-commit "$HEAD_SHA" "$PR_URL"

  enable-operator-dispatch:
    name: Enable native auto-merge (operator dispatch)
    if: >-
      github.event_name == 'workflow_dispatch' &&
      github.actor_id == '268063598' &&
      github.actor == 'lcv-leo' &&
      github.triggering_actor == github.actor &&
      github.run_attempt == 1 &&
      github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    environment:
      name: dependabot-automation
      deployment: false
    steps:
      - name: Create the repository-scoped GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.DEPENDABOT_AUTOMERGE_APP_CLIENT_ID }}
          private-key: ${{ secrets.DEPENDABOT_AUTOMERGE_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write
          permission-merge-queues: write

      - name: Resolve the eligible Dependabot pull request
        id: pull-request
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          PR_NUMBER: ${{ inputs.pull_request_number }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          [[ "$PR_NUMBER" =~ ^[1-9][0-9]*$ ]]
          pr_json="$(gh api "repos/${REPOSITORY}/pulls/${PR_NUMBER}")"
          jq -e --arg repository "$REPOSITORY" '
            .state == "open" and
            .draft == false and
            .base.ref == "main" and
            .head.repo.full_name == $repository and
            (.head.ref | startswith("dependabot/")) and
            .user.id == 49699333 and
            .user.login == "dependabot[bot]"
          ' <<<"$pr_json" >/dev/null
          head_sha="$(jq -er '.head.sha' <<<"$pr_json")"
          pr_url="$(jq -er '.html_url' <<<"$pr_json")"
          printf 'head_sha=%s\n' "$head_sha" >>"$GITHUB_OUTPUT"
          printf 'pr_url=%s\n' "$pr_url" >>"$GITHUB_OUTPUT"

      - name: Verify Dependabot provenance of the exact head
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          HEAD_SHA: ${{ steps.pull-request.outputs.head_sha }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          commit_json="$(gh api "repos/${REPOSITORY}/commits/${HEAD_SHA}")"
          jq -e --arg sha "$HEAD_SHA" '
            .sha == $sha and
            .author.id == 49699333 and
            .author.login == "dependabot[bot]" and
            .committer.login == "web-flow" and
            .commit.verification.verified == true and
            .commit.verification.reason == "valid"
          ' <<<"$commit_json" >/dev/null

      - name: Enable GitHub native auto-merge
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          HEAD_SHA: ${{ steps.pull-request.outputs.head_sha }}
          PR_URL: ${{ steps.pull-request.outputs.pr_url }}
        run: gh pr merge --auto --match-head-commit "$HEAD_SHA" "$PR_URL"
